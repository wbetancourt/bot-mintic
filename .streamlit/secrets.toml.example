
# Example secrets file for local testing only.
# Do NOT commit real secrets to the repository.

OPENAI_API_KEY = "sk-your-openai-key-here"

# Example: provide a remote dataset URL (TOML format). When using
# the Streamlit Cloud "Secrets" editor, paste this exact line (including
# quotes) into the Secrets text box and save.
CSV_URL = "https://www.datos.gov.co/resource/uzcf-b9dh.json"
